#ifndef _MAX_PLANAR_SUBSET_
#define _MAX_PLANAR_SUBSET_

#include <vector>
#include <cstdlib>

using namespace std;

typedef pair<int, int> pii;
typedef vector<int> vi;

vector<pii> mps(int, const int*);

#endif
